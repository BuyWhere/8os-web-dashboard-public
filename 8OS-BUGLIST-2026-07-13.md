# 8os — CONSOLIDATED BUG DOSSIER (2026-07-13, compiled by hermes-ops)

Richmond wants these fixed NOW. The deploy pipeline is verified working as of today
(frontend AND backend deploy green — every fix you push actually ships within ~2 min).
Deploy: commit in THIS dir is fine for railway up (your token is restored), or use the
canonical repo + git push vault main. After each fix, verify live at 8os.ai.

## P0/critical — user-facing, fix first
- **OS-2733** (critical): Fix Lighthouse desktop performance regression (84 to 90+) - unblocks 2026-07-14 PH launch
- **OS-3511** (high): Missing DNS for cal.8os.ai and telly.8os.ai — services unreachable
- **OS-3501** (high): Deploy OS-3498 PostHog perf fix to production
- **OS-3500** (high): Fix /alignment auth bypass + retire legacy /auth/register and /auth/verify on FastAPI backend
- **OS-3498** (high): Fix dash.8os.ai mobile perf regression (Lighthouse 73, LCP 5.7s) — gate is 90
- **OS-3102** (high): Restore api.8os.ai /waitlist/count routing (still 404 while sibling routes are healthy)
- **OS-2712** (high): Fix 4 WCAG accessibility failures on production home (drop 8os.ai Accessibility 89% → 95%+)
- **OS-2229** (high): Cloudflare DNS records missing for tududi.richteo.com and tududi-bridge.richteo.com (subdomain NXDOMAIN)
- **OS-2016** (high): [Drift evidence 12:22Z] api.8os.ai/waitlist/count regresses 200→404 — CF worker degradation widening
- **OS-1754** (high): Discord Connector Migration & Fixes
- **OS-388** (high): Set up performance monitoring: UptimeRobot + Lighthouse CI for 8os.ai

## Known specifics with full context
1. **Auth/security**: OS-3500 — /alignment auth bypass + legacy /auth/register + /auth/verify still exposed on the FastAPI backend. Backend now boots (users table renamed app8os_users; PORT=8000) — retire the legacy routes.
2. **Waitlist count 404** (OS-3102/OS-2016): api.8os.ai/waitlist/count returns 404 while siblings work — likely CF worker route config, drift-engine has evidence in the issue.
3. **Google Calendar Connect**: scopes in src/lib/auth/google.ts line ~146 are 'openid email profile' — MISSING https://www.googleapis.com/auth/calendar.events, so two-way sync can never write. Also env names: app reads GOOGLE_CLIENT_ID/GOOGLE_CLIENT_SECRET/GOOGLE_REDIRECT_URI (redirect = https://8os.ai/api/sources/google/callback).
4. **Login page JS exceptions** (OS-3353): 2 empty exceptions on /login — Clerk SDK or PostHog init.
5. **Pricing contrast** (OS-2779): Free-tier disabled feature text near-invisible.
6. **WCAG on home** (OS-2712): 4 accessibility failures.
7. **Lighthouse perf** (OS-2733 critical, OS-3498 mobile: LCP 5.7s): regression, PH launch gate is 90.

## Findings-store entries for 8os (QA evidence)
- [p2/promoted] Every /archetypes/famous/<name> page prefetches /famous RSC which returns 404
- [p1/promoted] /signin direct route returns 404
- [p2/promoted] Contact page Developer docs link opens /developers 404

## Ground rules
- Fix → deploy → VERIFY LIVE at 8os.ai before moving on. The pipeline works; trust but verify.
- The backend DB is SHARED with cal.com + a Paperclip instance + the frontend Prisma schema, all in public. Backend tables: app8os_users, waitlist_entries (shared with frontend! 269 real rows), referral_codes. Don't touch cal.com tables.
- There are TWO parallel referral systems (backend referral_codes vs frontend referral_clicks/attributions) — flag for Richmond, don't unify unilaterally.
- Mark each fixed item here with [SHIPPED <sha>] so Richmond can track.

## Quinn LOGGED-IN pass — 2026-07-13T08:52Z (14 bugs: 4×P0, 6×P1, 4×P2)
Logged in as quinn@theopsin.com (Clerk signup + email verification + 7-step onboarding all worked).

**[P0-1] React Error #425 hydration mismatch on Dashboard**
- Page: https://8os.ai/dashboard
- Console: `Minified React error #425` — Text content mismatch between server and client
- This is a genuine hydration bug causing client re-renders; user-visible symptoms include layout flicker and potential state inconsistency
- Evidence: `.playwright-mcp/console-2026-07-13T08-50-25-205Z.log`
- Fix: Inspect all dynamic content rendered on the dashboard that differs between SSR and client (timestamps, AI-generated text, "Your daily brief" greeting). Ensure server and client produce identical initial HTML.

**[P0-2] React Error #422 hydration mismatch**
- Page: multiple (dashboard/briefing/inbox/etc.)
- Console: `Minified React error #422` — similar text content mismatch
- Evidence: same log file
- Fix: same root cause investigation as P0-1

---

## P1 — MAJOR

**[P1-1] Calendar event created but not visible in week view until page refresh**
- Page: https://8os.ai/calendar
- Repro: Create event "QA created calendar event" at 8:45 AM → event appears in text extraction and day/month views, but event text appeared truncated in week view body text
- Expected: Event should be immediately visible in the week view without navigation
- Actual: After creating, the event appeared in day/month but not confirmed in week grid body text — potential rendering issue with newly created events in week view
- Screenshot: 8os-calendar-event-edit.png, 8os-calendar-week.png
- Fix: Verify week-view event re-render after create; check if event is properly committed to DB before render or if there's a stale fetch

**[P1-2] Dashboard shows no greeting name — "Your daily brief" instead of personalized greeting**
- Page: https://8os.ai/dashboard
- Expected: "Good morning, Quinn" or similar based on archetype/timezone
- Actual: Heading shows "Your daily brief" (generic), no first name in the greeting area
- Evidence: Dashboard body text shows heading "Your daily brief" with no personalization; also snapshot ref=f2e833
- Fix: Wire the user's first name (from onboarding "Quinn QA") into the dashboard greeting

**[P1-3] "AI insight unavailable today" on Daily Briefing**
- Page: https://8os.ai/dashboard/briefing
- Body text: `⚡ AI insight unavailable today — using a curated fallback.`
- Expected: AI-generated daily insight or graceful empty state without explicit "unavailable" messaging
- Actual: Visible degradation message — confusing for new users who don't know if the app is broken
- Screenshot: 8os-briefing.png
- Fix: Either remove the "unavailable" label (just show the curated fallback without explanation) or suppress the block entirely if no real data

**[P1-4] Settings → Profile: email shows "(unverified)" despite completing Clerk verification**
- Page: https://8os.ai/settings/profile
- Evidence: `quinn@theopsin.com (unverified)` in the email address list
- Expected: Email should show as verified after entering the 6-digit code during signup
- Actual: "(unverified)" label persists on the profile page
- Fix: Clerk verification status may not be synced to the profile page, or the email verification step didn't set the verified flag correctly

**[P1-5] Default timezone is UTC with no auto-detect on Preferences**
- Page: https://8os.ai/settings/preferences
- Evidence: Timezone dropdown defaults to "UTC" with the label "We detected UTC." — but the user is likely not in UTC
- Expected: Browser timezone auto-detect should populate the correct timezone on first visit (or show a more prominent "this detected UTC, click to change" prompt)
- Actual: User is likely in a different timezone (implied by server location) but UTC is pre-selected
- Screenshot: 8os-settings-preferences.png shows the full timezone list
- Fix: Use `Intl.DateTimeFormat().resolvedOptions().timeZone` on page load to pre-populate the correct timezone before showing the form

---

## P2 — POLISH

**[P2-1] "lifeare" typo — missing space in onboarding Goals heading**
- Page: https://8os.ai/onboarding/goals
- Evidence: Heading rendered as "What areas of your lifeare you focused on?" (no space between "life" and "are")
- Snapshot ref: f2e225
- Fix: Add space in the string literal: `"lifeare"` → `"life are"`

**[P2-2] Archetype page CTA text contradicts next-step copy**
- Page: https://8os.ai/onboarding/archetype
- Evidence: Button says "Build my OS →" but paragraph below says "Next: choose the areas of life your OS will focus on"
- The "Next: choose..." text implies the next step is Goals, which is correct — but it's confusing UX
- Fix: Change paragraph to "Next: set your goals" or remove the "Next:" line entirely since the CTA button is sufficient

**[P2-3] Calendar: multiple ⚡ Auto-schedule buttons for unscheduled tasks**
- Page: https://8os.ai/calendar
- Evidence: 10 identical "⚡ Auto-schedule" buttons shown for 10 unscheduled tasks — button text "⚡ Auto-schedule" has no
